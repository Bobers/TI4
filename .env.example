# Supabase Configuration for Browser (Safe to expose)
# Get these from your Supabase project settings under API
# The VITE_ prefix makes these available in the browser build
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# ⚠️ SERVER-SIDE ONLY - NEVER expose to browser or Vercel environment variables!
# These are only for local scripts and data uploads
# DO NOT add SUPABASE_SERVICE_KEY to Vercel!
SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_KEY=your-supabase-service-role-key